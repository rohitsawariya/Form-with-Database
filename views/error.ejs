<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error</title>
  <style>
    @keyframes pulse {
      0% {
        transform: scale(1);
        opacity: 1;
      }
      50% {
        transform: scale(1.1);
        opacity: 0.7;
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }
    body {
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: black;
      color: #ffffff; 
      font-family: 'Arial', sans-serif;
      text-shadow: 0px 4px 6px rgba(0,0,0,0.3);
    }
    div {
      text-align: center;
      padding: 20px;
      max-width: 600px;
      box-shadow: 0px 0px 52px #f27979;
      animation: pulse 2s infinite ease-in-out;
    }
  
    h1:first-of-type {
      margin-bottom: 20px;
      font-size: 2.5em;
      color: #ff6347; 
    }
    h1:last-of-type {
      margin-top: 0;
      font-size: 2em;
      color: white;
    }
    div::before {
      content: "⚠️";
      font-size: 5em;
      display: block;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div>
    <h1>Error</h1>
    <h1>Something Went Wrong</h1>
  </div>
</body>
</html>
